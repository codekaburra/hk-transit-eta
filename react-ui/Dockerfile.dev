# Dev image — runs npm start with HMR on port 3000
FROM node:18-alpine
WORKDIR /app
# Install deps into the image — node_modules lives here, NOT in the mounted volume.
# This prevents macOS/Linux binary incompatibilities.
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
